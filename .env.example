# 部署环境变量示例（复制为 .env 后按需修改）

# HTTP 端口（宿主机映射端口）
PORT=3000

# JWT 签名密钥 —— 生产环境必须改成随机长字符串
JWT_SECRET=CHANGE_ME_IN_PRODUCTION

# AI 供应商：deepseek（需 key）/ fake（无 AI，全部走关键词检索降级）
AI_PROVIDER=fake
DEEPSEEK_API_KEY=

# AI 成本护栏
AI_TIMEOUT_MS=20000
AI_MAX_OUTPUT_TOKENS=1000
# 调用次数预算，0 = 不限制
AI_MAX_CALLS=0
