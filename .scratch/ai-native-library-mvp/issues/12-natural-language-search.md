# 12 — 自然语言检索（含降级）

**What to build:** 免登录自然语言检索（LLM 语义理解）→ 相关度排序结果 + 引用依据（命中题名/匹配理由）；AI 不可用或超时自动降级为关键词检索。离线评测集随本 ticket 交付。

**Blocked by:** 09, 10

**Type:** task

**Status:** ready-for-agent

- [ ] 自然语言查询返回相关度排序结果
- [ ] 结果附引用依据（命中题名、匹配理由）
- [ ] AI 不可用/超时自动降级为关键词检索且体验不中断
- [ ] 离线评测：golden 查询集达到约定检索命中率
- [ ] 检索旅程在浏览器 seam 验证（自然语言查询 → 结果 → 架位指引）
