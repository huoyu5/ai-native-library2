# 10 — AI 供应商抽象层

**What to build:** AI 供应商抽象接口 + 默认国内模型实现；调用限额（超时、单查询上限）；AI 操作审计日志（时间、操作者、输入、输出）。

**Blocked by:** 02

**Type:** task

**Status:** ready-for-agent

- [ ] 供应商可切换（默认国内模型），业务接口不随供应商变化
- [ ] 超时/超限调用被拒绝并记录
- [ ] 每次 AI 调用写入审计日志（含操作者）
- [ ] 在 API seam 验证
